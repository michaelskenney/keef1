# v0.2 Design — Timeline Questions, Script Throttling, Content Expansion

**Date:** 2026-03-15
**Status:** Approved

## Overview

Three workstreams to ship v0.2: a new timeline question type, image fetch script hardening, and ~100 new questions to double the question bank.

## Workstream 1: Timeline Question Type

### Data shape (in questions.json)

```json
{
  "id": "tl-001",
  "type": "timeline",
  "category": "albums",
  "question": "Put these albums in order of release (earliest first)",
  "albums": [
    { "name": "Sticky Fingers", "year": 1971, "image": "/images/albums/sticky-fingers.jpg" },
    { "name": "Exile on Main St.", "year": 1972, "image": "/images/albums/exile-on-main-st.jpg" },
    { "name": "Some Girls", "year": 1978, "image": "/images/albums/some-girls.jpg" }
  ],
  "points": 10
}
```

- `albums` stored in correct chronological order; app shuffles at display time
- Correctness derived by comparing player's order against sort-by-year
- No separate `answer` field needed

### UI interaction

- 3 cards in a vertical stack, each showing album cover thumbnail + album name
- Player drags cards up/down to reorder (custom pointer events — works on touch and mouse)
- "Lock In" button submits the answer
- Feedback screen shows correct order with years revealed

### Scoring (partial credit)

- Each album in correct position = `floor(basePoints / 3)` = 3 pts each
- Speed bonus: `floor(correctPositions / 3 * timeRemaining / totalTime * maxSpeedBonus)`
- 0 correct = 0 points, no speed bonus

### Type system changes

- New `TimelineQuestion` interface: `type: 'timeline'`, `albums: {name, year, image}[]`, no `answer` field
- Add `'timeline'` to `QuestionType` union and `Question` discriminated union

### Question selection

- Timeline questions fall under `albums` category weight
- No guaranteed slot (unlike lyrics/image) — enters the random pool

## Workstream 2: Image Fetch Script Throttling

Changes to `scripts/fetch_images.py`:

- **1.5s sleep** between Wikimedia API requests (Wikimedia rate limit guideline)
- **Retry with exponential backoff** on 429/timeout errors (3 attempts: 2s → 4s → 8s)
- **Batch pause** — 5s pause between member groups

## Workstream 3: Content Expansion

Target: ~100+ new questions (217 → ~317 total).

| Category | Current | New  | Target | Types |
|----------|---------|------|--------|-------|
| Lyrics   | 8       | ~40  | ~48    | Fill-blank, free-text |
| Albums   | 72      | ~20  | ~92    | Timeline (~15), MC, fill-blank |
| Trivia   | 56      | ~25  | ~81    | MC, fill-blank, free-text |
| Members  | 81      | ~15  | ~96    | MC, image |
| **Total**| **217** |**~100**|**~317**| |

Key decisions:
- Lyrics gets the largest share to fix category imbalance (8 → ~48)
- ~15 new timeline questions created with a new Python script
- Existing scripts reused where possible
- All new images from Wikimedia Commons (CC-licensed) via throttled fetch script

## Current Question Bank Stats

- 217 total questions
- Albums: 72, Members: 81, Lyrics: 8, Trivia: 56
- Types: MC 81, Fill-blank 63, Free-text 33, Image 40
